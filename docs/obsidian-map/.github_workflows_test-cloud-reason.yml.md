# Test Cloud Reason

- **Путь**: `.github\workflows\test-cloud-reason.yml`
- **Тип**: .YML
- **Размер**: 2004 байт
- **Последнее изменение**: 1772680654.4408183

## Предпросмотр

```
name: Test Cloud Reason Component

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Validate component-config.yaml
        run: |
          python -c "
          import yaml
          with open('component-config.yaml', 'r', encoding='utf-8') as f:
              config = yaml.safe_load(f)
          print('✅ YAML валиден!')
       
... (файл обрезан для предпросмотра)
```
